[project]
name = "vosu"
version = "0.1.0"
description = "AI Conversational Copilot — real-time earbud assistant (offline, local GPU)"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["backend", "."]
asyncio_mode = "auto"
asyncio_default_fixture_loop_scope = "function"
# Test treo là lỗi, không phải "đang chạy lâu". Không có mốc này, một bản giả
# lập lệch interface có thể làm cả bộ test đứng im vô hạn — đã xảy ra thật khi
# FakeLlm thiếu build_prompt.
timeout = 60
filterwarnings = ["error::DeprecationWarning:app.*"]

[tool.ruff]
line-length = 100
target-version = "py310"
src = ["backend", "benchmarks", "tests"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "ASYNC"]
ignore = ["E501", "B008"]
