# --- Python ---
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.ruff_cache/

# --- Model weights: KHÔNG commit (nhiều GB) ---
models/*
!models/.gitkeep
!models/README.md

# --- Kết quả benchmark: sinh ra khi chạy, không commit ---
benchmarks/results/*
!benchmarks/results/.gitkeep

# --- Cấu hình cục bộ ---
.env
# config.yaml ĐƯỢC commit trên nhánh release-mac: nó chính là thứ phân biệt
# nhánh này với release. Ghi đè cục bộ dùng config.local.yaml hoặc VOSU_*.
config.local.yaml

# --- Audio mẫu ---
*.wav
*.mp3
!tests/fixtures/*.wav

# --- OS / editor ---
.DS_Store
.idea/
.vscode/
